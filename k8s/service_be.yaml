apiVersion: v1
kind: Service
metadata:
  name: backend
spec:
  type: NodePort         
  selector:
    app: chatbot-backend
  ports:
    - protocol: TCP
      port: 8000         
      targetPort: 8000   
      nodePort: 32000    
